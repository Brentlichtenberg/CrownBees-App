# This `.xcode.env` file is versioned and used to resolve the location of the Node binary.
# See https://reactnative.dev/docs/environment-setup#optional-configuring-your-environment

# To setup manually: `node --print "process.execPath"` and set that result as NODE_BINARY
export NODE_BINARY=$(command -v node)
